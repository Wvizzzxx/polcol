# План разработки сайта ГАПОУ ВО «Владимирский политехнический колледж»

## ✅ Завершено

### Блок 1: Бэкенд (Node.js + Express + MongoDB)
- [x] Создана структура `server/` с моделями, контроллерами, роутами
- [x] Модели: User, News, Page, Navigation, Employee, Specialty, Media, Contact, ContactMessage, Document, Settings, Event
- [x] JWT-аутентификация и роли (superadmin, admin, editor, viewer)
- [x] Middleware: загрузка файлов (Multer), проверка прав (JWT)
- [x] Seed-скрипт для создания админа + начальных данных (навигация, страницы, контакты, настройки, новости)
- [x] API роуты для всех сущностей + дашборд

### Блок 2: Фронтенд админ-панели
- [x] Установлены зависимости: React Query, react-hook-form, react-hot-toast, recharts
- [x] Настроен Vite proxy (`/api` → `http://localhost:5000`)
- [x] AuthContext (JWT, токен, роли)
- [x] useApi хук для удобных API-запросов
- [x] AdminLayout + Sidebar с навигацией
- [x] DataTable — переиспользуемая таблица
- [x] Страница логина `/admin/login`
- [x] Дашборд со статистикой `/admin`
- [x] CRUD страницы для всех модулей (15+ модулей)
- [x] Кнопки "На сайт" и "Выйти" в сайдбаре
- [x] Интеграция с основным приложением (main.jsx, App.jsx)
- [x] Сборка проходит успешно

### Блок 3: Перевод основного сайта на данные из API
- [x] Создан публичный API-хук `usePublicApi`
- [x] **Header** — загружает навигацию из API (`/api/navigation`) с падением на хардкод
- [x] **SubPage** — загружает контент из API (`/api/pages/by-path?path=...`) с падением на `pagesContent.js`
- [x] **News** — загружает новости из API (`/api/news`) с падением на хардкод
- [x] **Contacts** — загружает контакты из API (`/api/contacts`), форма отправляет сообщения на `POST /api/contacts/messages`
- [x] **Footer** — загружает контакты/настройки из API (`/api/contacts`, `/api/settings`) с падением на хардкод

### Блок 3.5: Улучшения админ-панели
- [x] **DataTable** — поиск/фильтрация по всем колонкам
- [x] **DataTable** — постраничная навигация (пагинация) с настраиваемым размером страницы
- [x] **DataTable** — модальные окна для подтверждения удаления (вместо нативного confirm)
- [x] **DataTable** — иконка редактирования (SVG-карандаш) с переходом на /admin/{module}/:id
- [x] **Все CRUD-списки** — добавлен `onEdit` через `useNavigate`, убран нативный `confirm()` из `handleDelete`

### Блок 4: Исправления и доработки
- [x] Исправлен `pre('save')` хук в User.js — убран `next()` для async-функции
- [x] Исправлен `pre('save')` хук в News.js — убран `next()` (новая версия Mongoose)
- [x] Исправлен route `/by-path/*` → `/by-path?path=...` (path-to-regexp v8 не поддерживает wildcard)
- [x] Исправлен `getByPath` контроллер — теперь принимает `req.query.path`
- [x] Обновлён `usePublicApi.js` — использует query-параметр вместо path
- [x] Расширен `seed.js` — добавлены начальные данные: навигация, страницы, контакты, настройки, новости

---

## 🔜 Осталось сделать

### 1️⃣ Тестирование (самое важное)
- [x] Запустить сервер: `cd server && npm run seed && npm run dev`
- [ ] Проверить вход в админку (`/admin/login` — admin@vpk.ru / admin123)
- [ ] Проверить CRUD операции для каждого модуля (создать/редактировать/удалить)
- [ ] Проверить загрузку файлов в медиа-менеджере
- [ ] Проверить работу публичного сайта (главная, страницы, новости, контакты)
- [ ] Проверить что данные подгружаются из API (а не из хардкода)

### 2️⃣ Деплой (опционально)
- [ ] Настроить production сборку
- [ ] Настроить переменные окружения (MongoDB, JWT_SECRET)